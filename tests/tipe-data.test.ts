describe('Data Type', function(): void {
  it('should must declare', function(): void {
    let name: string = 'Fuad Muhammad Nur';
    let balance: number = 1000;
    let isMarried: boolean = false;

    console.info(name);
    console.info(balance);
    console.info(isMarried);
  });
});